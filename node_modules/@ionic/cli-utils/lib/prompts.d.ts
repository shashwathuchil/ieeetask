import { PromptQuestion } from '@ionic/cli-framework';
import { ILogger, IonicEnvironmentFlags } from '../definitions';
export interface CreateOnFallbackOptions extends IonicEnvironmentFlags {
    readonly log: ILogger;
}
export declare function createOnFallback({ confirm, interactive, log }: CreateOnFallbackOptions): (question: PromptQuestion) => string | boolean | void | string[];
